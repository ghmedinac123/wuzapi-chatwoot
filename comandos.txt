# Reconstruir y desplegar solo integration
docker compose build integration
docker compose up -d integration

# Ver logs
docker compose logs -f integration

# DESARROLLO (con hot-reload) ✅
uv run uvicorn main:app --host 0.0.0.0 --port 8789 --reload

# PRODUCCIÓN (sin reload) ✅
uv run python main.py